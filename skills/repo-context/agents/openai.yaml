interface:
  display_name: "Repo Context"
  short_description: "Map frontend repo structure before work"
  default_prompt: "Use $repo-context to identify this frontend repo's scripts, layers, aliases, and verification path."
